name: Master CI/CD
on:
  push:
    tags:
      - v*
permissions:
  id-token: write
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - uses: ./.github/workflows/release.yml
