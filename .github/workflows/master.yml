name: Master CI/CD
on:
  push:
    tags:
      - v*
permissions:
  id-token: write
  contents: read
jobs:
  test:
    environment: dev
    runs-on: ubuntu-latest
    steps:
      - uses: phitran/phi-test/.github/workflows/release.yml@main
